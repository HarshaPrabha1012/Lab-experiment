#include<stdio.h>
#include<string.h>
int main()
{
char str1[25],str2[25];
printf("Enter two strings:");
scanf("%s",&str1);
for(int i=0;str1[i]!='\0';i++)
{
    str2[i]=str1[i];
}
{
    printf("%s",str2);
}
    return 0;
    
}

